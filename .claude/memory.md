# Space Manager Memory

## Checkpoint #53 — 2026-01-19 14:09

### 📖 Эпизод (что произошло)
Продолжили оптимизацию Space Manager. Сначала пользователь жаловался что Ctrl+` открывает окно спустя 5 секунд и показывает неправильный активный Space. Нашли причину — `refresh_aerospace_cache()` использовал `os.system()` внутри Qt event loop что вызывало блокировку. Добавили `get_focused_workspace()` для определения активного workspace из AeroSpace (`list-workspaces --focused`). Потом обнаружили что focused workspace тоже зависает при вызове из Qt — пришлось перенести в pre-cache. Создали `update_focused_workspace_sync()` которая вызывается до Qt. Убрали все блокирующие вызовы aerospace из Qt — теперь `refresh_apps_from_cache()` только читает кэш. После этого Ctrl+` стал быстрым! Но возникла новая проблема — перемещения окон работали только в UI, реально окна не двигались (точнее двигались, но кэш не обновлялся). Добавили `update_window_workspace_in_cache()` которая обновляет workspace окна в кэше после move. В конце пользователь заметил что куча окон уехала в правый нижний угол — это AeroSpace что-то сделал. Пользователю AeroSpace не понравился.

### 💡 Ключевые знания
- **О пользователе:** Ларри не нравится AeroSpace — слишком агрессивно управляет окнами, двигает их куда не надо
- **Технические:** `aerospace list-workspaces --focused` — получить текущий активный workspace
- **Критическое:** ЛЮБОЙ вызов aerospace CLI из Qt event loop вызывает зависание, даже из отдельного потока!
- **Паттерн:** Все данные aerospace должны кэшироваться ДО создания QApplication

### 🔧 Процедуры и методы
- ✅ **Что сработало:** `refresh_apps_from_cache()` — мгновенное обновление UI из кэша без блокировки
- ✅ **Что сработало:** `update_window_workspace_in_cache()` — обновление кэша после move без вызова aerospace
- ✅ **Что сработало:** Pre-cache focused workspace в `precache_aerospace_windows()`
- ❌ **Что не сработало:** Вызов `update_focused_workspace_sync()` из hotkey потока — всё равно блокирует
- ❌ **Что не сработало:** AeroSpace как window manager — слишком агрессивно управляет окнами

### 🎯 Текущее состояние
**Достигнуто:** Ctrl+` открывается мгновенно, правильный активный Space
**Проблемы:** AeroSpace двигает окна в угол, пользователю не нравится
**Следующие шаги:** Отключить AeroSpace, найти альтернативный способ перемещения окон

### 🔗 Связи
Продолжение Checkpoint #52 — оптимизация скорости открытия. Checkpoint #51 — та же проблема с зависанием aerospace CLI.

---

## Checkpoint #52 — 2026-01-19 02:35

### 📖 Эпизод (что произошло)
Исправили глюки UI после перемещения окон! Проблема была в том, что после move делался полный `refresh_apps()` через QTimer.singleShot(1000), который пересоздавал все виджеты и вызывал визуальные артефакты. **Решение:** Заменили полный рефреш на мгновенные точечные обновления — скрываем перемещённое окно в source карточке через `setVisible(False)`, добавляем новый виджет в target карточку через `_add_window_to_card()`, показываем зелёную подсветку успеха.

### 💡 Ключевые знания
- **UI:** Полный refresh вызывает glitching из-за пересоздания всех виджетов
- **Паттерн:** Мгновенные точечные обновления UI лучше полного rebuild
- **Методы:** `setVisible(False)` + `_add_window_to_card()` + `_show_success_flash()`

### 🔧 Процедуры и методы
- ✅ **Что сработало:** Instant UI updates вместо полного refresh
- ✅ **Что сработало:** Context Menu теперь тоже без глюков
- ✅ **Что сработало:** Drag-n-drop плавный

### 🎯 Текущее состояние
**Достигнуто:** Перемещение окон работает плавно через context menu и drag-n-drop
**Window IDs:** Корректно получаются из AeroSpace (видно в логах wid=124737 и т.д.)

---

## Checkpoint #51 — 2026-01-19 02:15

### 📖 Эпизод (что произошло)
Решили критическую проблему с зависанием aerospace CLI в Qt-приложении! Aerospace работал из терминала за 0.027 сек, но зависал бесконечно при вызове из Python-процесса Qt. Пробовали: subprocess.run с разными timeout, QProcess, shell wrapper через /bin/sh, ThreadPoolExecutor — всё таймаутилось. **Решение:** Предварительное кэширование (pre-cache) окон **ДО создания QApplication!** После этого и ThreadPool заработал внутри Qt. Добавили `precache_aerospace_windows()` в начало main(). Теперь drag-n-drop и context menu должны работать.

### 💡 Ключевые знания
- **Критическое:** Aerospace CLI зависает при вызове из Qt event loop контекста
- **Решение:** Pre-cache aerospace windows ПЕРЕД созданием QApplication
- **Паттерн:** `_aerospace_worker()` + `ThreadPoolExecutor` для subprocess в Qt
- **Qt плагины:** Нужен `export QT_PLUGIN_PATH="/opt/anaconda3/lib/python3.12/site-packages/PyQt6/Qt6/plugins"`

### 🔧 Процедуры и методы
- ✅ **Что сработало:** Pre-cache перед Qt — `precache_aerospace_windows()` в начале main()
- ✅ **Что сработало:** ThreadPoolExecutor для subprocess внутри Qt (после pre-cache)
- ❌ **Что не сработало:** QProcess.waitForFinished() — тоже зависает
- ❌ **Что не сработало:** subprocess.Popen с start_new_session=True
- ❌ **Что не сработало:** Shell wrapper через /bin/sh -c

### 🎯 Текущее состояние
**Достигнуто:** Aerospace CLI работает, кэширование окон работает, move_window_to_space готов
**Готово к тестированию:** Context Menu (правый клик → "Переместить на Space X") и drag-n-drop

---

## Checkpoint #50 — 2026-01-19 01:55

### 📖 Эпизод (что произошло)
Продолжили отладку drag-n-drop для перемещения окон между Spaces в Space Manager. Сначала долго бились с проблемой что события мыши не доходили до WindowItemWidget — оказалось Qt не видел виджеты из-за прозрачного фона. Попробовали разные подходы: изменили фон на непрозрачный, переделали WindowItemWidget с QFrame на QPushButton — и события наконец заработали! Видели в логах [HOVER] Enter и [DRAG] Accepted. Потом обнаружили что Window ID возвращается как 0 — окна не находились. Выяснили что AeroSpace использует **свои** window ID, отличные от Quartz CGWindowNumber! AeroSpace показывает ID типа 124737, а Quartz — 124736, и даже title разные. Переписали get_window_id_by_title чтобы сначала искать через `aerospace list-windows --all`. Добавили также Context Menu (правый клик → "Переместить на Space X") как альтернативу drag-n-drop. Сейчас новая проблема — aerospace list-windows таймаутит за 2 секунды.

### 💡 Ключевые знания
- **Технические:** AeroSpace window ID ≠ Quartz CGWindowNumber! Это разные системы
- **Команда:** `aerospace list-windows --all` — формат "ID | App | Title"
- **Команда:** `aerospace move-node-to-workspace N --window-id ID`
- **Qt:** Прозрачный фон rgba(0,0,0,0.01) делает виджет невидимым для hit-testing
- **Qt:** QPushButton надёжнее QFrame для получения mouse events

### 🔧 Процедуры и методы
- ✅ **Что сработало:** QPushButton вместо QFrame для WindowItemWidget
- ✅ **Что сработало:** Context Menu через contextMenuEvent
- ✅ **Что сработало:** AeroSpace для перемещения окон (не требует SIP)
- ❌ **Что не сработало:** Quartz CGWindowNumber для AeroSpace
- ❌ **Что не сработало:** WA_TransparentForMouseEvents на контейнере — сломало все события
- ❌ **Что не сработало:** aerospace list-windows с timeout=2 — слишком мало

---
